<% provide(:title, '全ユーザー') %>
<h1>全ユーザー</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= render @users %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>