<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><span><%= link_to current_user.name, current_user %></span></h1>
<span><%= pluralize(current_user.microposts.count, "投稿") %></span>